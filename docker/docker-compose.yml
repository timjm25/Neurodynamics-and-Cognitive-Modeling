version: "3.9"
services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ../data:/app/data
      - ../output:/app/output
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
